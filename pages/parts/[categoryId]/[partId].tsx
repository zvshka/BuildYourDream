import { Paper, Stack, Title } from '@mantine/core';
import { Block } from '../../../components/Block/Block';

export default function partPage() {
  return (
    <Stack>
      <Block>
        <Title order={2}>PART TITLE</Title>
      </Block>
    </Stack>
  );
}
